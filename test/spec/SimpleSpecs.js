describe("This is a basic spec without much going on.", function() {

  it("Should expect false to be false.", function() {
    expect(false).toBe(false);
  });

  it("Should expect some answer to be some answer.", function() {
    expect(44).toBe(44);
  });

});

